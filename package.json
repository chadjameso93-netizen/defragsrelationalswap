{
  "name": "defragsrelationalswap",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "install:all": "pnpm install -r",
    "dev": "pnpm --dir apps/web dev",
    "build": "pnpm --dir apps/web build",
    "start": "pnpm --dir apps/web start",
    "typecheck": "pnpm --dir apps/web typecheck",
    "test": "pnpm run lint:copy && vitest run",
    "lint:copy": "node scripts/copy-guard.mjs"
  },
  "devDependencies": {
    "next": "15.5.14",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
